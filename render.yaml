services:
  - type: web
    name: https://proyecto-peliculas-wnuw.onrender.com
    env: virtual_environment
    runtime: python3.11
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000